import GlobalAnimatedWarMap from "@/components/map/GlobalAnimatedWarMap";

export default function WarMapPage() {
  return (
    <GlobalAnimatedWarMap
      title="War Command Center"
      subtitle="Track the live global theater, spot pressure before it spreads, and give players a world state they want to monitor throughout the day."
    />
  );
}
