import GlobalAnimatedWarMap from "@/components/map/GlobalAnimatedWarMap";

export default function ClubsMapPage() {
  return (
    <GlobalAnimatedWarMap
      title="Club Territory Map"
      subtitle="Scout every frontline, inspect rewards, and launch coordinated invasions from the same animated global view used across the app."
    />
  );
}
